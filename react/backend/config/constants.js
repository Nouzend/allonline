const DB_HOST = "localhost";
const DB_USER = "root";
const DB_PASSWORD = "";
const DB_DATABASE = "7-11all";
const DB_PORT = 3306;

const SEVER_PORT = 4000;

const JWT_SECRET = "bukobayol";
const JWT_EXPIRE_TIMEOUT = 3600;

const ADMIN_ROLE = "admin";
const USER_ROLE = "user";

module.exports = {
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_DATABASE,
  DB_PORT,
  SEVER_PORT,
  JWT_SECRET,
  JWT_EXPIRE_TIMEOUT,
  ADMIN_ROLE,
  USER_ROLE,
};
